// The matchers `vitest.setup.ts` installs at run time, declared for the type checker: a
// specification in this package asserts with them, so this program has to know about them.
import '@testing-library/jest-dom/vitest'
