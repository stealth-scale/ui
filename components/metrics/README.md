# @stealthscale/component-metrics

A **library**: Draws a number about a thing, and the tiles that hold a wall of them.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
