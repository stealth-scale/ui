# @stealthscale/component-layout

A **library**: Arranges what is already there: the boxes, the stacks, the rules between them and the surfaces they sit on.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
