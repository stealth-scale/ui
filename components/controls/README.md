# @stealthscale/component-controls

A **library**: Draws the things a person operates, one at a time: a button, a field, a slider, a switch.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
