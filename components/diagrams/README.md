# @stealthscale/component-diagrams

A **library**: Draws relationships between nodes: graphs, flows, lineage and pipelines.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
