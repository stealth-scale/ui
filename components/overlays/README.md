# @stealthscale/component-overlays

A **library**: Draws what floats above the page: dialogs, menus, popovers, tooltips and sheets.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
