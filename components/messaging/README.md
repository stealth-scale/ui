# @stealthscale/component-messaging

A **library**: Draws a conversation, whether the other side is a person or a model.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
