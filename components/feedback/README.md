# @stealthscale/component-feedback

A **library**: Draws the system reporting on itself: what is loading, what went wrong, what state a thing is in.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
