# @stealthscale/component-typography

A **library**: Draws the components that are text: a heading, a paragraph, a snippet of code, a key a reader is told to press.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
