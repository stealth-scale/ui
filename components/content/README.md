# @stealthscale/component-content

A **library**: Presents a body of something: markdown, a diff, a document, a record about a thing.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
