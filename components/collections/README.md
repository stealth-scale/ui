# @stealthscale/component-collections

A **library**: Draws many of a thing: lists, trees, tables and the grids that render them.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
