# @stealthscale/component-navigation

A **library**: Draws the ways a person moves between places: links, tabs, breadcrumbs and rails.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
