# @stealthscale/component-charts

A **library**: Plots values on axes, and owns the charting library that does it.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
