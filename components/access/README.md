# @stealthscale/component-access

A **library**: Draws who may do what: gates, approvals and the record of both.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
