# @stealthscale/component-forms

A **library**: Composes controls into something a person submits, and reports what is wrong with it.

Empty until its first component lands. The build script and `src/index.ts` arrive with it.
